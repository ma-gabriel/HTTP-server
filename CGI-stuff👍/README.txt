

Salut les gens.

Dans la partie CGI pour l'instant il y aura des trucs marqués en brut dans le code
genre que si ca trouve un code python ca utilise /usr/bin/python3.10
mais c'est le genre de trucs qui se trouve dans le fichier de config,
donc je le changerait plus tard

Egalement, l'emoji dans le nom du directory, c'est pour pas oublier de changer ca,
mais pareil, ce sera quand il y aura des fichiers de config + des fichiers HTML,
des pages d'erreurs, etc. Bref, je me comprends



D'AILLEURS. Fun fact: sur vim vous pouvez transformer un fichier en HTML 
avec la commande toHTML si vous voulez créer vos CGI

Aller je retourne bosser
