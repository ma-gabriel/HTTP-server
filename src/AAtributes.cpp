//// Created by rperrot on 5/20/25.//#include "AAtributes.hpp"#include <algorithm>#include <iostream>#include <string>AAtributes::AAtributes():_autoIndex(false), _maxBodySize(0){}AAtributes::~AAtributes(){}AAtributes::AAtributes(const AAtributes &a){    this->_autoIndex = a._autoIndex;    this->_maxBodySize = a._maxBodySize;    this->_cgi = a._cgi;    this->_autoIndex = a._autoIndex;    this->_httpMethode = a._httpMethode;    this->_errorPages = a._errorPages;    this->_root = a._root;}AAtributes::AAtributes(const std::string &root, const std::map<int, std::string> &error_pages, const std::vector<EHttpMethode> &http_methode, const bool auto_index, const std::vector<std::string> &index)        : _root(root), _errorPages(error_pages), _httpMethode(http_methode), _autoIndex(auto_index), _index(index){}const std::string &AAtributes::getRoot() const{    return _root;}const std::map<int, std::string> &AAtributes::getErrorPages() const{    return _errorPages;}const std::vector<EHttpMethode> &AAtributes::getHttpMethode() const{    return _httpMethode;}const std::vector<std::string> &AAtributes::getIndex() const{    return _index;}void AAtributes::setRoot(const std::string &root){    _root = root;}void AAtributes::setHttpMethode(const std::vector<EHttpMethode> &httpMethode){    _httpMethode = httpMethode;}void AAtributes::setErrorPages(const std::map<int, std::string> &errorPages){    _errorPages = errorPages;}void AAtributes::setIndex(const std::vector<std::string> &index){    _index = index;}void AAtributes::setAutoIndex(bool autoIndex){    _autoIndex = autoIndex;}bool AAtributes::isAutoIndex() const{    return _autoIndex;}char toLowerChar(char c) {    return std::tolower(static_cast<unsigned char>(c));}bool AAtributes::addAttributes(std::vector<std::string>::iterator &it, const std::vector<std::string>::iterator &end){    typedef void (AAtributes::*Action)(std::vector<std::string>::iterator &, int);    std::map<std::string, Action> actions;    actions["root"] = &AAtributes::addRoot;    actions["autoindex"] = &AAtributes::addAutoIndex;    actions["index"] = &AAtributes::addIndex;    actions["error_page"] = &AAtributes::addErrorPages;    actions["limit_except"] = &AAtributes::addHttpIndex;    actions["client_max_body_size"] = &AAtributes::addClientMaxBodySize;    actions["cgi"] = &AAtributes::addCgi;    std::transform(it->begin(), it->end(), it->begin(),                  toLowerChar);    if (it == end) {        std::cerr << "Error: No attributes found." << std::endl;        return false;    }    if (actions.find(*it) == actions.end()) {        return false;    }    Action itAction = actions[*it];    int n = getSizeLine(it, end);    (this->*itAction)(it, n);    return true;}void AAtributes::addRoot(std::vector<std::string>::iterator &it, int n) {   if (n != 2) {       throw std::runtime_error("Root attribute requires a value.");   }   this->_root = (*(++it));}void AAtributes::addClientMaxBodySize(std::vector<std::string>::iterator &it, int n) {    if (n != 2) {        throw std::runtime_error("Root attribute requires a value.");    }    if (!strIsDigit(*(++it))) {        throw std::runtime_error("Client max body size must be a digit.");    }    double maxBodySizeDouble = std::strtod((*it).c_str(), NULL);    this ->_maxBodySize = static_cast<int>(maxBodySizeDouble);}void AAtributes::addIndex(std::vector<std::string>::iterator &it, int n) {   if (n == 1) {       throw std::runtime_error("index attribute requires a value.");   }   std::vector<std::string> index;    for (int i = 1; i < n; ++i) {         index.push_back(*(++it));    }    this->_index = index;}void AAtributes::addCgi(std::vector<std::string>::iterator &it, int n) {   if (n == 1) {       throw std::runtime_error("Cgi attribute requires least one value.");   }   std::vector<std::string> cgi;    for (int i = 1; i < n; ++i) {         cgi.push_back(*(++it));    }    this->_cgi = cgi;}void AAtributes::addAutoIndex(std::vector<std::string>::iterator &it, int n) {    if (n != 2) {        throw std::runtime_error("AutoIndex requires on or off.");    }    if (strcmpNocase(*(++it), "on")) {        this->_autoIndex = true;    } else if (strcmpNocase(*(it), "off")) {        this->_autoIndex = false;    } else {        throw std::runtime_error("AutoIndex attribute must be 'on' or 'off'.");    }}void AAtributes::addHttpIndex(std::vector<std::string>::iterator &it, int n) {    for (int i = 1; i < n; ++i) {        if (strcmpNocase(*(++it), "get")) {            this->_httpMethode.push_back(Get);        } else if (strcmpNocase(*(it), "post")) {            this->_httpMethode.push_back(Post);        } else if (strcmpNocase(*(it), "delete")) {            this->_httpMethode.push_back(Delete);        } else if (strcmpNocase(*(it), "put")) {            this->_httpMethode.push_back(Put);        } else {            throw std::runtime_error("HTTP method must be 'GET', 'PUT', 'POST' or 'DELETE'.");        }    }}void AAtributes::addErrorPages(std::vector<std::string>::iterator &it, int n) {    std::vector<int> errorCodes;    if (n < 3 ) {        throw std::runtime_error("Error pages attribute requires pairs of error code and page.");    }    for (int i = 1; i < n - 1; ++i) {        if (!strIsDigit(*(++it))) {            throw std::runtime_error("Error code must be a digit.");        }        double errorDouble = std::strtod((*it).c_str(), NULL);        int errorCode = static_cast<int>(errorDouble);        if (errorCode < 100 || errorCode > 599) {            throw std::runtime_error("Error code must be between 100 and 599.");        }        if (this->_errorPages.find(errorCode) != this->_errorPages.end()) {            throw std::runtime_error("Duplicate error code: " + *it);        }        errorCodes.push_back(errorCode);    }    it++;    for (std::vector<int>::iterator itError = errorCodes.begin(); itError != errorCodes.end(); ++itError) {        this->_errorPages.insert(std::make_pair(*itError, *it));    }}int AAtributes::getSizeLine(std::vector<std::string>::iterator it, const std::vector<std::string>::iterator &end) {    int n = 0;    while (it != end && *it != ";") {        it++;        n++;    }    if (it == end || *it != ";") {        throw std::runtime_error("Expected ';' at the end of the line.");    }    return n;}const std::vector<std::string> &AAtributes::getCgi() const {    return _cgi;}int AAtributes::getMaxBodySize() const {    return _maxBodySize;}std::ostream &operator<<(std::ostream &os, const AAtributes &attributes) {    os << "Root: " << attributes.getRoot() << "\n";    os << "AutoIndex: " << (attributes.isAutoIndex() ? "on" : "off") << "\n";    os << "Error Pages:\n";    for (std::map<int, std::string>::const_iterator  it = attributes.getErrorPages().begin();         it != attributes.getErrorPages().end(); ++it) {        os << "\t" << it->first << ": " << it->second << "\n";    }    os << "HTTP Methods: ";    for (std::vector<EHttpMethode>::const_iterator it = attributes.getHttpMethode().begin(); it != attributes.getHttpMethode().end(); ++it) {        os << getMethodString(*it) << " ";    }    os << "\nIndex: ";    for (std::vector<std::string>::const_iterator it = attributes.getIndex().begin(); it != attributes.getIndex().end(); ++it) {        os << *it << " ";    }    os << "\ncgi: ";    for (std::vector<std::string>::const_iterator it = attributes.getCgi().begin(); it != attributes.getCgi().end(); ++it) {        os << *it << " ";    }    os << "\nMax Body Size: " << attributes.getMaxBodySize() << " bytes";    os << "\n";    return os;}